import React from 'react'

const Sort = () => {
  return (
      <form>
          <input type={'radio'} value={'name'} checked={} onChange={handle}></input>
          <input type={'radio'} value={'birthDate'} checked={} onChange={handle}></input>
   </form>
  )
}

export default Sort
